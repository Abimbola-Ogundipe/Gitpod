I created an sdk folder with two sub directories, Ruby and Java
For Ruby, I created a Ruby installer filr name it install "./sdk/Ruby/install"
Then installed bundler with sudo -gem install bundler 
This will create a gemfile and then in this Gemfile i will add the aws sdk necessary to install to this file.
I then run sudo bundle install to intall this sdks using -sudo bundle istall
I added client package in the s3.rb file 
